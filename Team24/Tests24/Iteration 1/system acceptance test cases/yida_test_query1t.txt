1 - Select p such that Uses(p, v) // Select Procedures that use variable
assign a; procedure proc;
Select v such that Uses(proc, v)

100
2 - Select p such that Uses(p, v) // Select Procedures that use variable
stmt s; assign a; procedure proc;
Select v such that Uses(s, v)

100
3 - Select p such that Uses(p, v) // Select Procedures that use variable
stmt s; assign a; procedure proc;
Select v such that Uses(s, v) such that Uses(a, v)

100
4 - Select p such that Uses(p, v) // Select Procedures that use variable
stmt s; assign a; procedure proc;
Select v such that Uses(a, v)

100