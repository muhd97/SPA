1 - Select a such that Uses(a, v) // Select Assigns that use a variable
assign a; variable k;
Select a such that Uses(a, _)
3, 8
25
2 - Select a such that Uses(pr, v) // Select Prints that use a variable
assign a; variable k; print pr, rp, rr, pp;
Select pp such that Uses(pp, _)
9, 5
25
3 - Select v such that Uses(c, v) // Select Calls that use variable
call k;
Select k such that Uses(k, _)
11, 12
25
4 - Select p such that Uses(p, v) // Select Procedures that use variable
procedure k;
Select k such that Uses(k, _)
yida1, yida2, yida3
25